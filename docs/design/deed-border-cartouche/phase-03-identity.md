# Phase 3 starter: identity and family

Model: grok-4.6. Reasoning: xhigh. Do not start until Phase 2 is marked
complete in `progress.md`.

## Goal

Per-slug side motifs, Catalogue brass retune, Book of Deeds picker
swatches, inspect cartouche, portrait-ring clasp. Decisive tests for
matrix rows E27-E36. Phase 1 rows must stay green.

## Where to work

`/Users/fernando/Documents/wocc-deed-border-cartouche` on
`feature/deed-border-cartouche`.

## Read first

`implementation-plan.md` Phase 3, `state.md`, `src/ui/CLAUDE.md`,
`src/styles/CLAUDE.md` (if touching CSS), `DESIGN.md` 1 and 4,
`docs/design/graphics-settings-fairness.md`,
`tests/deeds_border_picker.test.ts`, `tests/unit_frame_painter.test.ts`,
`src/styles/hud.css` portrait-ring rule, `src/styles/shell.css`
inspect-name rule.

## Authorized

Motif kind on `BorderAccent`, four shape sets in the cartouche core,
`curators_gilt` retune, inspect/ring/picker family, fairness and design
doc sentences, shot-target recipes for picker and inspect, the listed
tests, `progress.md` / `state.md`.

## Not authorized

Phase 4 screenshot commitment (QA owns the album). New slugs or deeds.
Animated sparkle. Wrapping the health bar. Unique silhouettes per slug.
Sim / server / IWorld. Commits unless asked.

## Checks

The Phase 3 validation block in `implementation-plan.md`. Catalogue
must not collide with `reliquary_gilt` or `#f2c84b`. Picker keeps
`.deed-title-option` and the 40x40 mobile floor. Ring clasp stays under
the level chip.

## Handoff

`state.md` points at `phase-04-qa-identity.md`.
