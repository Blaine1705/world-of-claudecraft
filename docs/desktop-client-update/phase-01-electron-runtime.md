# Phase 1: Electron runtime plumbing

### Starter Prompt
```
This is Phase 1 of the Desktop Client Update: Electron runtime plumbing.

Model: Opus 4.8, xhigh effort. Harness: Claude Code. ULTRACODE: not needed (small phase).

PROJECT RULES (non-negotiable, from docs/desktop-client-update/state.md):
- Work ONLY in /home/fernandoramirez/Documents/woc-desktop-client-update (branch
  feature/desktop-client-update); every git command uses `git -C` with that path.
- LOCAL-ONLY: never push, never open a PR.
- First action: `git -C <worktree> fetch origin release/v0.36.0 && git -C <worktree>
  merge origin/release/v0.36.0`; on a non-trivial merge re-run the electron/desktop
  suites before new work. `git status --porcelain` must be clean before starting.

Goal: move the Electron toolchain to current patches and enable the V8 code cache for
the app:// scheme, with pins.

STEP 0 - PRE-FLIGHT: rules above, then memory scan (topics: desktop-client-update
program, pnpm install cosmetics, worktree cwd drift).

STEP 1 - LOAD CONTEXT: spawn an Explore agent (hard budget ~25 tool calls, report-first)
to read and summarize: docs/desktop-client-update/state.md, progress.md, this phase
file; electron/main.cjs registerAppProtocol + registerSchemesAsPrivileged region;
scripts/electron-vendor.mjs; the test files tests/electron_ipc_channels.test.ts (house
text-scan style) and one pure-module test for shape reference; package.json
devDependencies + build block. Return: the exact current scheme privileges object, how
vendor bundling is invoked, and which tests pin main.cjs content.

STEP 2 - EXECUTE (single agent or inline; this phase is small):
Deliverables:
- Bump deps via pnpm ONLY (never hand-edit the lockfile):
  `pnpm -C <worktree> add -D electron@^43.3.0 electron-builder@^26.15.7`. Verify
  installed versions resolve to 43.3.0 / 26.15.7.
- Re-run `node scripts/electron-vendor.mjs` (electron-log/electron-updater did not
  move, so the vendor bundles must come out unchanged; verify with git status).
- Add `codeCache: true` to the app:// privileges in registerSchemesAsPrivileged in
  electron/main.cjs (requires standard:true, already set). Do NOT add stream or any
  other privilege.
- Add a text-scan pin (house style, alongside the existing main.cjs scans) asserting
  the privileges object carries standard, secure, supportFetchAPI, corsEnabled, AND
  codeCache all true, so a future refactor cannot silently drop one.

INVARIANTS IN PLAY: tiny-deps (no new packages); no other dependency moves; never
commit lockfile changes you cannot explain; no em dashes/emojis.

Out of scope: Electron 44; any other package.json change; touching the protocol handler
logic or media_range; show/menu/second-instance work (phase 2).

STEP 3 - VALIDATION + REVIEW:
- `pnpm -C <worktree> install --frozen-lockfile` sanity (cosmetic "-136" line is known).
- `npx vitest run tests/electron_*.test.ts tests/desktop_*.test.ts`
- `npx tsc --noEmit`
- `npm run electron:pack` (website channel, ad-hoc). If the packaged app launches in
  this environment, launch it and capture main.log's startup banner + gpu line; if the
  environment blocks a GUI launch, record that explicitly rather than skipping silently.
- Review dispatch per the matrix in docs/desktop-client-update/implementation-plan.md:
  this diff touches electron/ scheme privileges -> spawn privacy-security-review
  (budget ~30 tool calls, COVERAGE not filtering, report BLOCKING/SHOULD-FIX/
  NICE-TO-HAVE/VERDICT). No other row matches.
- `node scripts/gate_select.mjs` to close.

STEP 4 - COMMITS (explicit paths; Conventional Commits with a body):
- chore(desktop): bump electron to 43.3.0 and electron-builder to 26.15.7
- feat(desktop): enable V8 code cache for the app scheme

STEP 5 - ACCEPTANCE:
- [ ] electron 43.3.0 and electron-builder 26.15.7 installed; lockfile regenerated by
      pnpm; vendor bundles byte-identical.
- [ ] codeCache:true present in the privileges object; new pin fails if any privilege
      is dropped.
- [ ] All electron/desktop suites green; gate_select green; pack smoke recorded.

STEP 6 - DOCS + MEMORY: update progress.md (phase 1 row + checklist) and state.md
inventory (dependency moves; the pin you added). Note surprises in memory.

STEP 7 - FINAL RESPONSE: phase status, files touched, validation results, reviewer
verdict, deferrals, one-line handoff for phase 1 QA.

STOPPING RULES: stop and ask if the electron bump changes runtime behavior any existing
test pins (do not weaken a pin to get green); stop if pack fails for a reason unrelated
to this diff.
```
