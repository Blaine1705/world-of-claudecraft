# Desktop Client Update: whole-packet integration QA matrix (phase 11)

Verified once, at packet completion, in the worktree
/home/fernandoramirez/Documents/woc-desktop-client-update.

- [ ] Three-host neutrality: this packet is client/shell-only. `git diff --name-only`
      against the packet-start base shows NO changes under `src/sim/`, `server/`,
      `headless/`, `python/`, or `src/world_api*`. Any hit is a scope breach to explain
      or revert.
- [ ] Determinism untouched: architecture guard green
      (`npx vitest run tests/architecture.test.ts`).
- [ ] i18n completeness: every new player-visible string is a t() key in the English
      catalog; wordy keys carry their five non-Latin fills (M16); the shell string
      contract pins pass (`npx vitest run tests/desktop_shell_strings.test.ts`); the S3
      guard passes (`npx vitest run tests/localization_fixes.test.ts`); no locale
      overlay files were edited beyond M16 fills.
- [ ] Graphics fairness: nothing added hides or delays actionable information; tier
      knobs still read the static preset only; the hidden-window skip renders nothing
      only while hidden; the LOW retune is a load change, not an information change.
- [ ] Shell security posture unchanged: sandbox/contextIsolation/fuses/CSP/permissions
      as at base; every new IPC handler is trustedSender-gated within its first 200
      chars (`npx vitest run tests/electron_ipc_channels.test.ts`); no new escape
      hatches into packaged config; the prefs store validates and clamps everything it
      reads; no secrets, no .env changes.
- [ ] Dependency scope: only electron, electron-builder, three, @types/three,
      postprocessing, n8ao moved; zero NEW dependencies (Rich Presence is in-house);
      pnpm-lock.yaml regenerated by pnpm only.
- [ ] Perf evidence bundle: phase 5 monotonicity numbers, phase 6 baseline-vs-after
      perf:baseline and perf:crowd comparison (no preset regresses avg FPS or 1% lows
      beyond the 5% tolerance recorded in state.md; improvements documented), hidden
      window GPU-idle evidence from phase 4.
- [ ] Visual acceptance: phase 6 before/after screenshots reviewed; the r181 lighting
      shift explicitly accepted by the user or compensated.
- [ ] Desktop features degrade cleanly: every bridge consumer no-ops on web/mobile and
      on an older shell missing the new methods; Discord absence produces silent
      backoff, no boot impact.
- [ ] Update/packaging: electron:pack succeeds for the website channel locally; the
      updater track stamps and channel gating are untouched
      (`npx vitest run tests/electron_updater_track.test.ts tests/electron_update_guard.test.ts`).
- [ ] Copy review: no em dashes, en dashes, or emojis anywhere in the diff.
- [ ] Full gate green: `npm run gate` in the worktree (record the known environmental
      browser-test failure on this machine if it fires; PR CI is the arbiter).
- [ ] Docs: progress.md and state.md complete and truthful; deferred items listed;
      memory topic file updated.
- [ ] Teardown offered: ask the user explicitly before deleting
      docs/desktop-client-update/; delete only on confirmation, only that directory.
- [ ] LOCAL-ONLY intact: zero pushes happened; the branch has never left this machine.
